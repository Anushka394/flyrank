
POSTGRES_USER=app
POSTGRES_PASSWORD=app_password
POSTGRES_DB=appdb

# Used by the app container when running via docker compose (host = service name "db")
DATABASE_URL=jdbc:postgresql://db:5432/appdb
DB_USER=app
DB_PASSWORD=app_password